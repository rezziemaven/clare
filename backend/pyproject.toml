[project]
name = "clare-backend"
version = "0.1.0"
description = "Backend for Clare app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
